[project]
name = "dbt-authorized-models-tests"
version = "0.2.1"
description = "Test tooling for dbt-authorized-models"
requires-python = ">=3.10"
dependencies = [
    "dbt-core>=1.12,<1.13",
    "dbt-duckdb>=1.11,<1.12",
]

[tool.uv]
package = false

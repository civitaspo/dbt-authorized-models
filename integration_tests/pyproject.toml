[project]
name = "dbt-authorized-models-integration-tests"
version = "0.1.0"
description = "Integration tests for dbt-authorized-models"
requires-python = ">=3.10"
dependencies = [
    "dbt-core>=1.10,<1.11",
    "dbt-duckdb>=1.10,<1.11",
]

[tool.uv]
package = false

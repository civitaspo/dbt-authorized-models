# dbt-authorized-models

`dbt-authorized-models` is a dbt package for enforcing explicit authorization rules on model references.

This repository is maintained as a public open source project. The initial implementation will add the package macros, documentation, and integration tests in a focused pull request.
